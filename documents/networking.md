# Networking

The application communicates with the backend using HTTP APIs.

The networking layer is responsible for making requests,
handling responses, and converting API errors into application errors.