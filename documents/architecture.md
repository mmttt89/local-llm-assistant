# Architecture

Our application uses a modular architecture.

The presentation layer is responsible for UI.
The domain layer contains business logic.
The data layer handles networking and persistence.

We prefer keeping business logic independent from the UI.